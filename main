from SourceHandler import sortFiles
import numpy as np
from scipy import misc

sortFiles("C:\\Users\Ben\Documents\GitHub\Image_Interpreter\Oranges")

data = np.load('0.npy') #Lodaing the .npy file
misc.toimage(data).show() #Showing the array as an image
misc.imsave('rgb_gradient.png', data) #Saving it as an image

input("Press enter to stop viewing the picture") #nessecary to keep the program running
